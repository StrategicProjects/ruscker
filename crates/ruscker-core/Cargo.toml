[package]
name = "ruscker-core"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Core domain logic for Ruscker: session management, routing, lifecycle"

# Pure-domain crate. No I/O, no async runtime. Async-capable traits
# are defined here but their implementations live in sibling crates.

[dependencies]
ruscker-config = { path = "../ruscker-config" }

async-trait = { workspace = true }
futures-util = { workspace = true }
serde = { workspace = true }
thiserror = { workspace = true }
uuid = { workspace = true }
chrono = { workspace = true }
