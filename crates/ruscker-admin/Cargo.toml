[package]
name = "ruscker-admin"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Admin panel for Ruscker (HTMX + Tailwind + Askama)"

# NOTE: this crate is a STUB. The real implementation (phase 2) will
# add axum, askama, sqlx, tower-http, etc. See CLAUDE.md in this
# crate for the full dependency list.

[dependencies]
ruscker-config = { path = "../ruscker-config" }
ruscker-core = { path = "../ruscker-core" }
anyhow = { workspace = true }
